@model IEnumerable<GetUsersViewModel>
@using Microsoft.Extensions.Localization
@using SimpleProject.Resources
@inject IStringLocalizer<SharedResources> _sharedResources;

<div>
    <p>
        <a asp-action="Create" class="btn btn-success">Create New</a>
    </p>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>
                    @_sharedResources[SharedResourcesKeys.Name]
                </th>
                <th>
                    Address
                </th>
                <th>
                    Email
                </th>
                <th>
                    Options
                </th>
            </tr>
        </thead>
        <tbody id="productList">
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Address)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email)
                    </td>
                    <td>
                        <a asp-action="Details" asp-controller="User" asp-route-id="@item.Id" class="btn btn-info"> details</a>
                        <a asp-action="Update" asp-controller="User" asp-route-id="@item.Id" class="btn btn-primary">Update</a>
                        <a asp-action="Delete" asp-controller="User" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                        <a asp-action="ManageRolesInUser" asp-controller="User" asp-route-userId="@item.Id" class="btn btn-secondary">Manage Roles</a>
                        <a asp-action="ManageUserClaims" asp-controller="User" asp-route-userId="@item.Id" class="btn btn-secondary">Manage Claims</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>